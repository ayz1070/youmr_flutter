import 'package:flutter/material.dart';

class AppColors {
  // 기본 색상 (Light Mode)
  static const Color primary = Color(0xFFC1AAF4);
  static const Color secondary = Color(0xFF03DAC6);
  static const Color background = Color(0xFFFFFFFF); // 흰색 배경
  static const Color surface = Color(0xFFF5F5F5); // 연한 회색 배경
  static const Color textPrimary = Color(0xFF000000); // 검은 텍스트
  static const Color textSecondary = Color(0xFF757575); // 보조 텍스트
  static const Color divider = Color(0xFFE0E0E0); // 구분선 색상
  static const Color error = Color(0xFFB00020);

  // 다크 모드 색상 (Dark Mode)
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkSurface = Color(0xFF1E1E1E);
  static const Color darkTextPrimary = Color(0xFFFFFFFF);
  static const Color darkTextSecondary = Color(0xFFB0B0B0);
  static const Color darkDivider = Color(0xFF303030);

  // 공통 색상
  static const Color white = Colors.white;
  static const Color black = Colors.black;
}